syntax = "proto3";

package bar;

message SpamRequest {
  string eggs = 1;
}

message SpamResponse {
  string banana = 1;
}

service SpamService {
  rpc Do(SpamRequest) returns (SpamResponse) {}
  rpc Stream(stream SpamRequest) returns (stream SpamResponse) {}
}
